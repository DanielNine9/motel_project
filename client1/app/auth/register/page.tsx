import Register from "@/components/auth/Register";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Register />
    </div>
  );
};

export default page;
